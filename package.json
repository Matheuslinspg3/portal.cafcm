{
  "name": "portal-cafcm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js --bundle --minify --target=es2022 --format=iife --outfile=dist/app.js",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.115.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
